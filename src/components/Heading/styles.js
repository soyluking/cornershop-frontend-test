import styled from 'styled-components';

export const SHeading = styled.header`
  padding: 0 1rem;
  text-align: center;
`;

export const SHeadingText = styled.div`
  color: var(--black);
  margin: 1.2rem 0;
`;
